/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/tarefas',
  assetPrefix: '/tarefas/',
  output: 'export', // Add this line
};

export default nextConfig;
